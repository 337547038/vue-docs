# Button 按钮

## 基础用法
:::demo Alert 组件提供四种主题，由`type`属性指定，默认值为`info`。

```html
<template>
  <Button>确认</Bbutton>
</template>
<script>
import Button from '../../src/components/button'
export default {
  name: 'Button0',
  components:{Button}
}
</script>
```
:::
